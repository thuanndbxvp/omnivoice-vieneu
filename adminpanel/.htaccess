# Chặn truy cập trực tiếp vào các file nhạy cảm ở root

<FilesMatch "^\.env(\..*)?$">
    Order deny,allow
    Deny from all
</FilesMatch>

# Forward tất cả request vào public/
RewriteEngine On
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ public/$1 [L]
